apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-go-server
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: simple-go-server
  template:
    metadata:
      labels:
        app: simple-go-server
    spec:
      containers:
      - name: simple-go-server
        image: mehedi02/simple-go-server:4f04855b2c90596998c9a4b013f0c67c3b66826f
        ports:
        - containerPort: 9090
        resources:
          requests:
            cpu: "100m"
            memory: "100Mi"
          limits:
            cpu: "500m"
            memory: "500Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: default
spec:
  type: NodePort
  selector:
    app: simple-go-server
  ports:
  - port: 9090
    targetPort: 9090
