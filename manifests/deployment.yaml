apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-go-server
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: simple-go-server
  template:
    metadata:
      labels:
        app: simple-go-server
    spec:
      containers:
      - name: simple-go-server
        image: mehedi02/simple-go-server:0acf2e8e506b7e0c7ef423d3d4f0c7fc3728e638
        ports:
        - containerPort: 9090
        resources:
          requests:
            cpu: "100m"
            memory: "100Mi"
          limits:
            cpu: "500m"
            memory: "500Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: default
spec:
  type: NodePort
  selector:
    app: simple-go-server
  ports:
  - port: 9090
    targetPort: 9090
